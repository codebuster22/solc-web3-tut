{
  "name": "compile-deploy-web3",
  "version": "1.0.0",
  "description": "Compile and deploy smart contracts using solc and web3",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "node scripts/compile.js",
    "deploy": "node scripts/deploy.js",
    "interact": "node scripts/interact.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "prompt-sync": "^4.2.0",
    "solc": "^0.8.5",
    "web3": "^1.3.6"
  }
}
